from abc import ABC, abstractmethod

class Executer(ABC):
    """
    The executer class should contain all logic with respect to where
    and with what resources different tasks should be run.
    """
    pass